export default function RolesPage() {
    return (
        <main className="min-h-screen bg-[#071426] p-6 text-white">
            <section className="mx-auto max-w-4xl rounded-lg bg-white p-6 text-slate-950">
                <h1 className="text-2xl font-bold">Roles</h1>
                <p className="mt-3 text-slate-600">
                    The backend currently has a roles controller shell. This page needs role
                    listing and role management after backend role endpoints are implemented.
                </p>
            </section>
        </main>
    );
}
